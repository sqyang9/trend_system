# Risk-Off Reentry Shortlist

## Status

`Phase R1` complete.

This file records the disciplined shortlist for the reopened Risk-Off core re-entry deep dive.

## Priority Order

1. `RO_REENTRY_STRUCTURE_REACCEPT_STAGE50_100`
2. `RO_REENTRY_TREND_REBUILD_STAGE50_100`
3. `RO_REENTRY_STRUCTURE_REACCEPT_FULL`
4. `RO_REENTRY_TREND_REBUILD_FULL`
5. `RO_REENTRY_TIME_PLUS_STATE_STAGE`

## Why This Order

- `RO_REENTRY_STRUCTURE_REACCEPT_STAGE50_100` is first because it is the cleanest direct test of the archived blocker: too much flat-state drag after conditions improve.
- `RO_REENTRY_TREND_REBUILD_STAGE50_100` is second because it is the best alternative if recovery behaves more like directional rebuild than local structure re-acceptance.
- `RO_REENTRY_STRUCTURE_REACCEPT_FULL` is third because it cleanly isolates whether staging itself is helping.
- `RO_REENTRY_TREND_REBUILD_FULL` is fourth because it risks collapsing into a slower cousin of the old EMA family.
- `RO_REENTRY_TIME_PLUS_STATE_STAGE` is fifth because it is useful as a control family, but most exposed to inheriting the old delay-heavy weakness.

## Why Each Is Disciplined

- No new trigger family search
- No sleeve logic changes
- No new asset classes
- No governance overlay repackaging
- No broad brute-force ladder

## Closed Directions

Remain closed:

- broad Risk-Off trigger redesign
- EMA neighborhood expansion as the main line
- broad close-hold `1/2/3/...` continuation
- broad staged percentage grids
- core-off-weight rewrite
- Bear Short expansion
- turning governance caution into trading logic

## R1 Judgment

- The opportunity-cost blocker is still the only valid reopened target.
- The next phase should test structure-led and trend-rebuild-led restoration before any wider re-entry family expansion is considered.
- Old simple hold-confirm designs remain reference benchmarks, not the new main hypothesis set.

## Direct Answers

1. Proposed re-entry designs:
   - `RO_REENTRY_STRUCTURE_REACCEPT_FULL`
   - `RO_REENTRY_STRUCTURE_REACCEPT_STAGE50_100`
   - `RO_REENTRY_TREND_REBUILD_FULL`
   - `RO_REENTRY_TREND_REBUILD_STAGE50_100`
   - `RO_REENTRY_TIME_PLUS_STATE_STAGE`

2. Which one should be tested first:
   `RO_REENTRY_STRUCTURE_REACCEPT_STAGE50_100`

3. Which old directions remain closed:
   broad trigger search, broad hold/stage brute-force, core-off-level rewrite, Bear Short coupling, and governance-style pseudo-signals.
