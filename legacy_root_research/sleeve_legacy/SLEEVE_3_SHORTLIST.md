# Sleeve 3 Shortlist

## Status

Pending `Phase T2`.

This file records the disciplined shortlist for the first `Sleeve #3` discovery round.

## Provisional T1 Priority Order

1. `reset_base_reacceptance`
2. `failed_breakdown_acceptance`
3. `dislocation_repricing_rebuild`
4. `trend_drift_persistence`

## Why This Order

- `reset_base_reacceptance` is first because it is the cleanest missing ecology relative to the current two-sleeve stack.
- `failed_breakdown_acceptance` is second because it is the strongest dislocation-specific alternative if reset / rebuild logic is truly what the portfolio still lacks.
- `dislocation_repricing_rebuild` is third because its potential is high, but the family is broader and more definition-sensitive.
- `trend_drift_persistence` is fourth because it may still collapse into a weaker relative of `Sleeve #1`.

## Immediate T2 Rejection List

Reject immediately as too similar to the current portfolio ecology:

- `compression_breakout` family variants
- `squeeze_release` variants
- `quality_breakout` variants
- `donchian breakout` lookback variants
- structure-confirm breakout variants
- breakout grading / cap / downgrade wrappers
- `range_rotation` micro-variants
- box-edge mean-reversion variants
- reclaim family repackagings of `pullback_reclaim_continuation`
- failed-breakout timing repairs that are still breakout cousins

## Current T1 Judgment

- The current two-sleeve stack already covers:
  - breakout / expansion
  - range rotation / chop diversification
- `Sleeve #3` should therefore focus on:
  - reset / rebuild acceptance
  - failed downside continuation
  - structural repricing after dislocation
  - slow persistence carry only if it is demonstrably distinct from breakout timing

## Direct Answers

1. What candidate should be tested first in T2?
   `reset_base_reacceptance`.

2. Which directions should be rejected immediately?
   Breakout near-cousins, range-rotation micro-variants, and reclaim / failed-break wrappers that are just renamed versions of already closed ecologies.
